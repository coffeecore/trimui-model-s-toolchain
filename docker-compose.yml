services:
    builder:
        build:
            context: .
            dockerfile: docker/Dockerfile
        container_name: trimui-builder
        working_dir: /workspace
        volumes:
            - .:/workspace
        stdin_open: true
        tty: true
