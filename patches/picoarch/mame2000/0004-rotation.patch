diff --git a/src/libretro/libretro.c b/src/libretro/libretro.c
index 37348a9..bb83307 100644
--- a/src/libretro/libretro.c
+++ b/src/libretro/libretro.c
@@ -74,6 +74,8 @@ int should_skip_frame                    = 0;
 static int sample_rate                   = 22050;
 static int stereo_enabled                = true;
 
+static int rotate                        = false;
+
 int game_index = -1;
 unsigned short *gp2x_screen15;
 /* Software-framebuffer fast path: when the frontend grants us a buffer of
@@ -413,6 +415,19 @@ static void update_variables(bool first_run)
             qsound_output_filter_enabled = 0;
     }
 
+    var.value = NULL;
+    var.key = "mame2000-rotate";
+
+    if (environ_cb(RETRO_ENVIRONMENT_GET_VARIABLE, &var) && var.value)
+    {
+        if(strcmp(var.value, "enabled") == 0)
+            rotate = true;
+        else
+            rotate = false;
+    }
+    else
+        rotate = false;
+
    /* Reinitialise frameskipping, if required */
    if (!first_run &&
        ((frameskip_type     != prev_frameskip_type)))
@@ -502,10 +517,17 @@ static void update_input(void)
 
 		key[KEY_1 + i]   |= JS(i, START);
 		key[KEY_5 + i]   |= JS(i, SELECT);
-		joy_pressed[c++] = JS(i, LEFT);
-		joy_pressed[c++] = JS(i, RIGHT);
-		joy_pressed[c++] = JS(i, UP);
-		joy_pressed[c++] = JS(i, DOWN);
+		if (rotate_controls) {
+			joy_pressed[c++] = JS(i, UP);
+			joy_pressed[c++] = JS(i, DOWN);
+			joy_pressed[c++] = JS(i, RIGHT);
+			joy_pressed[c++] = JS(i, LEFT);
+		} else {
+			joy_pressed[c++] = JS(i, LEFT);
+			joy_pressed[c++] = JS(i, RIGHT);
+			joy_pressed[c++] = JS(i, UP);
+			joy_pressed[c++] = JS(i, DOWN);
+		}
 		joy_pressed[c++] = JS(i, B);
 		joy_pressed[c++] = JS(i, A);
 		joy_pressed[c++] = JS(i, Y);
@@ -1121,6 +1143,11 @@ bool retro_load_game(const struct retro_game_info *info)
    options.samplerate = sample_rate;
    usestereo = stereo_enabled;
 
+   if (rotate) {
+      options.ror = 1;
+      rotate_controls = 1;
+   }
+
    /* This is needed so emulated YM3526/YM3812 chips are used instead on physical ones. */
    options.use_emulated_ym3812 = 1;
 
diff --git a/src/libretro/libretro_core_options.h b/src/libretro/libretro_core_options.h
index d215157..8f36eab 100644
--- a/src/libretro/libretro_core_options.h
+++ b/src/libretro/libretro_core_options.h
@@ -194,6 +194,21 @@ static struct retro_core_option_v2_definition option_defs_us[] = {
       },
       "enabled"
    },
+   {
+      "mame2000-rotate",
+      "Rotate (Restart)",
+      NULL,
+      "Rotate the game display and controls by 90 degrees. "
+      "Changing this value requires a core restart.",
+      NULL,
+      NULL,
+      {
+         { "disabled", NULL },
+         { "enabled",  NULL },
+         { NULL, NULL },
+      },
+      "disabled"
+   },
    {
       "mame2000-qsound_output_filter",
       "QSound Output Filter",
